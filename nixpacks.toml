# Nixpacks Configuration for Railway
# Python Telegram Bot Deployment

[phases.setup]
nixPkgs = ["python310"]

[phases.install]
cmds = ["pip install -r requirements.txt --no-cache-dir"]

[start]
cmd = "python bot.py"
